{
  "buildCommand": "next build",
  "installCommand": "npm install && pip install -r requirements.txt",
  "functions": {
    "api/rank.py": {
      "runtime": "python3.9"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}