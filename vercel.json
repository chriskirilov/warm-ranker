{
  "framework": "nextjs",
  "buildCommand": "next build",
  "installCommand": "npm install && pip install -r requirements.txt",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}